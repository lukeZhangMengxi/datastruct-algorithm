package patterns.dfs.binary_tree_path_sum;


public abstract class Solution {
    abstract public boolean hasPath(TreeNode root, int sum) throws Exception;

    protected void isValid(TreeNode root, int sum) throws Exception {
        
    }
}
