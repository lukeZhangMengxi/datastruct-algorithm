package patterns.linkedlist_inplace_reverse.reverse_sublist;


public abstract class Solution {
    abstract public ListNode reverseSubList(ListNode head, int p, int q) throws Exception;

    protected void isValid(ListNode head, int p, int q) throws Exception {
        
    }
}
